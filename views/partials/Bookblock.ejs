
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />
    <style>
      .mm {
        margin-left: 1%;

        border-bottom: none;
      }

      .btn_block {
        width: 100%;
        display: flex;
        justify-content: space-between;
      }

      .card-body {
        border-radius: 6px;
      }
      .card-body:hover {
        height: 40%;
        background-color: #39b39e;
      }
    </style>
  </head>
  <body>
    <div class="col-sm-2 mt-2 mb-2 mm">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title"><%=datas.title %></h2>
          <div class="card-text"><%=datas.author %></div>
          <div class="card-text"><%=datas.prize %></div>
          <h4><%=datas.category %></h4>
          <section class="btn_block mt-2">
            <button class="btn btn-primary" onclick="updates('<%=datas._id %>')">Update</button>
            <button class="btn btn-danger" onclick="deletes('<%= datas._id %>')">Delete</button>
          </section>
        </div>
      </div>
    </div>
    <script>
    
      function updates(e)
      {
        window.location.href="/update_ui/"+e;
          
      }

      const deletes = async(e)=>
      {
       
         window.location.href="/delete/"+e;
      }
    </script>
  </body>
</html>
